export const createPageUrl = (pageName: string): string => {
  // In React Native navigation, we just return the page name
  // This function maintains compatibility with the existing code
  return pageName;
};
